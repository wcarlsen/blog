[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
quart = "*"
hypercorn = "*"

[scripts]
watch = "hypercorn server:app --reload"
run = "hypercorn server:app"

[requires]
python_version = "3.8"
